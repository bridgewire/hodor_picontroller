# Git ignore file - specifies files excluded from repository

# ignore editor backup files
*~
# ignore Python compilation and build leftovers
*.py[cdo]
hodor_picontroller.egg-info
#
#  ignore files generated by watcher and slacker test runs
#
.hodor_lastslack
hodor_slacklog.txt
events
# the slacker config should be private and not checked in
.hodor_slacker_config.yml
# if Vagrant is used, ignore the .vagrant directory
.vagrant
